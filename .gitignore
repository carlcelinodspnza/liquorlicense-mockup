# Working artefacts — not part of the site the team is reviewing.
# Excluded on the owner's instruction to keep the clone small (240MB -> ~90MB).
# NOTE: .gitignore does NOT support trailing comments on a pattern line — the
# comment becomes part of the pattern and the rule silently matches nothing.

# 52 full-page screenshots, 100MB
_captures/

# superseded build artefacts, 38MB
_archive/

# per-block rollback points, 12MB
_backups/

_gate-receipts/
_webcard-renders/

.DS_Store
**/.DS_Store
Thumbs.db

# The rendered Service Area Register is INTERNAL ANALYSIS, not client-facing work: it
# documents defects in the client's own live site (duplicate pages, a competitor mention,
# a ChatGPT tracking artefact, coverage claims that exceed what they publish). The repo is
# the public preview, so publishing it there would put that analysis on a URL the client's
# own team browses. The DATA and the generators stay committed so it is reproducible with
# one command; only the rendered page is withheld. It is already published privately as an
# Artifact for the owner.
_geography/service-area-register.html

# Internal design exploration for the owner — six layout options for the service list.
# Not part of the site the client reviews. Reason kept on its OWN line: a trailing
# comment would become part of the pattern and the rule would match nothing.
_service-list-layouts.html

# Internal design exploration for the owner — six layout options for the service
# DETAIL band (services 01-04). Not part of the site the client reviews.
_service-detail-layouts.html

# Internal design exploration — layout options for services 05-08.
# Not part of the site the client reviews.
_service-0508-layouts.html

# Internal design exploration — layout options for the California services page.
# Not part of the site the client reviews.
_ca-services-layouts.html

# Internal design exploration — layout options for the market x type pages.
# Not part of the site the client reviews.
_sf20-layouts.html

# Internal design exploration — FAQ band layout options.
_faq-layouts.html

# Internal design exploration — illustrated band layout options.
_band-layouts.html

# local layout-option preview, not part of the site
_service-page-layouts.html

# transient image contact sheet
_contact-sheet.html

# transient crop test sheet
_crop-test.html

# transient hero crop test
_hero-crop-test.html

# local where-band layout options preview
_where-layouts.html

# local cross-link rail layout options preview
_rail-layouts.html

# local new-business covers band options preview
_newbusiness-covers-layouts.html

# local N-A text area options preview
_na-text-options.html

# transient type image sheet
_type-imgs.html

# local classification card layout preview
_class-card-layouts.html

# local preview of the restructured Locations mega menu (CA / FL / AZ) and the
# extracted block it renders. Internal review artefacts, not part of the site.
_mega-preview.html
_mm-new-block.html

# the two rendered mega-menu variants under review (markets vs classifications)
_mega-markets.html
_mega-classifications.html
_mm-new-block-markets.html
_mm-new-block-classifications.html

# local layout-option preview for the buy page's intro band
_buy-intro-layouts.html

# local layout-option preview for the state pages' counties/cities band
_geo-layouts.html
_geo-layouts-arizona.html
_geo-layouts-florida.html

# local preview of the buy-page form treatments
_form-layouts.html
