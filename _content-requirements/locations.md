# Content requirements — `locations.html` (markets & coverage)

Facts only. No IA, no SEO, no visual direction. The row below is a VERBATIM COPY of its row in
`index.md`, which is the MASTER registry — this file exists because
`verify-content-requirements-bound` resolves per page and a declared id whose row is absent here
(or carries no `; src:`) is treated as UNVERIFIED and hard-blocks the Write.

Claim ownership across pages: `_dedup-ledger.md` PART 1.

**This page OWNS:** claim **C33** — statewide reach, the all-58-counties fact, and the market list.
Ownership MOVED here from `index.html#coverage` on 2026-07-29 when the page was added; the homepage
band keeps its map, its chips and one line, plus a route through. That is the same summary-vs-full
split already used for services, licensing, inventory and FAQ.

**This page MUST NOT restate:** any Type 21/47/48/41/20 DEFINITION (owner: `licence-types.html`,
claims C18–C20) — it may NAME a type and link to it, never define what it authorises; the listing
details themselves (owner: `inventory.html`, C34) — it may show a per-market COUNT and link, never
reproduce a card's price/status copy; the cost number (owner: `faq.html`, C28); the transfer phases
(owner: `process.html`, C14–C17); which sector needs which type (owner: `index.html#industries`,
C21) — the qualifier may ASK the user their industry, but the page must not publish a sector→type
table.

**Note:** the 14 tabs are 1 statewide + 13 markets. Only 7 markets hold live stock. The other 6 are
advertised coverage with ZERO inventory and MUST carry the "we broker here · no live listings today"
treatment routing to a sourcing brief — never a bare market name a visitor could read as available
stock. `san-bernardino` is the inverse case: it holds a real listing but appears on neither the
client's published market list nor the homepage coverage band, so it is surfaced with that noted.

required_ids: [R-STAT-03]

---

R-STAT-03 | stat_metric | "Counties served" — All 58 California counties. ; src: _intake.md §5 (pdf)
